package com.example.stanley.alarm;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.util.DisplayMetrics;
import android.view.WindowManager;

/**
 * Created by yshui on 11/5/15.
 */
public class PopAlarm extends AppCompatActivity
{
    @Override
    protected void onCreate(Bundle savedInstanceState)
    {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.pop_alarm);
//        DisplayMetrics dm = new DisplayMetrics();
  //      getWindowManager().getDefaultDisplay().getMetrics(dm);
//
  //      double width = dm.widthPixels * 0.5;
    //    double height = dm.heightPixels * 0.4;
//
  //      getWindow().setLayout((int) width, (int) height);
//
        //       View viewer = findViewById(android.R.id.content);

  //      WindowManager.LayoutParams wlp = getWindow().getAttributes();
    //    wlp.flags = WindowManager.LayoutParams.FLAG_DIM_BEHIND;
      //  wlp.dimAmount = (float) 0.5;
        //getWindow().setAttributes(wlp);
    }


}
